<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="co.aspirasoft.sams">

  <application
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:logo="@drawable/logo"
      android:theme="@style/AppTheme">

    <activity android:name=".ui.activity.ActivitySplash">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name=".ui.activity.ActivitySignIn"/>

    <activity android:name=".ui.activity.ActivitySignUp"/>

    <activity android:name=".ui.activity.ActivityUserType"/>

    <activity android:name=".ui.activity.ActivitySchedule"/>

    <activity android:name=".ui.activity.ActivityDashboard"/>

  </application>

</manifest>